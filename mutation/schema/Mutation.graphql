type Mutation {
    # Virgula é opcional
    novoUsuario(
        dados: UsuarioInput!
    ): Usuario!
    
    excluirUsuario(
        filtro: UsuarioFiltro!
    ): Usuario
    
    alterarUsuario(
        id: Int!, 
        nome: String, 
        email: String, 
        idade: Int
    ): Usuario
}