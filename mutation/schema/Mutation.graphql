type Mutation {
    # Virgula é opcional
    novoUsuario(
        dados: UsuarioInput!
    ): Usuario!
    
    excluirUsuario(id: Int!): Usuario
    
    alterarUsuario(
        id: Int!, 
        nome: String, 
        email: String, 
        idade: Int
    ): Usuario
}